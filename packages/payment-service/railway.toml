[build]
builder = "NIXPACKS"
buildCommand = "npm run build:shared && npm run build --workspace=packages/payment-service"

[deploy]
startCommand = "node packages/payment-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
