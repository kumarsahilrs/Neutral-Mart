[build]
builder = "NIXPACKS"
buildCommand = "npm run build:shared && npm run build --workspace=packages/search-service"

[deploy]
startCommand = "node packages/search-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
