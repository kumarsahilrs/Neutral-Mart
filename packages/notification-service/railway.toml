[build]
builder = "NIXPACKS"
buildCommand = "npm run build:shared && npm run build --workspace=packages/notification-service"

[deploy]
startCommand = "node packages/notification-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
