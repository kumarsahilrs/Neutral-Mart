[build]
builder = "NIXPACKS"
buildCommand = "npm run build:shared && npm run build --workspace=packages/dispute-service"

[deploy]
startCommand = "node packages/dispute-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
