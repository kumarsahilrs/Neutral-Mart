[build]
builder = "NIXPACKS"
buildCommand = "npm run build:shared && npm run build --workspace=packages/inventory-service"

[deploy]
startCommand = "node packages/inventory-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
